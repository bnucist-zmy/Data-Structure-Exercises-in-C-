线性表 (Linear List)
一、线性表的定义和特点
1.定义  n（≥0）个数据元素的有限序列，记作（a1, a2, …, an），ai 是表中数据元素，n 是表长度。
2.特点   
（1）线性排列
（2）除第一个元素外，其他每一个元素有一个且仅有一个直接前趋。
（3）除最后一个元素外，其他每一个元素有一个且仅有一个直接后继。
二、理解线性表的要点
（1）表中元素具有逻辑上的顺序性，在序列中各元素排列有其先后次序，有唯一的首元素和尾元素。
（2）表中元素个数有限。
（3）表中元素都是数据元素。即每一表元素都是原子数据，不允许“表中套表”。
（4）表中元素的数据类型都相同。这意味着每一表元素占有相同数量的存储空间。

顺序表 (Sequential List)
一、顺序表的定义和特点
1.定义  将线性表中的元素相继存放在一个连续的存储空间中，即构成顺序表。
2.存储  它是线性表的顺序存储表示，可利用一维数组描述存储结构。
3.特点  元素的逻辑顺序与物理顺序一致。
4.访问方式  可顺序存取，可按下标直接存取

该习题要求实现顺序表的一系列操作，如初始化、求顺序表长度、判断是否为空、创建顺序表、插入、查找、删除等，

函数清单如下：
1.初始化：void initList ( SeqList & L)
2.长度函数：int Length(SeqList& L)
3.判空：bool isEmpty(SeqList& L)
4.判满：bool isFull(SeqList& L)
5.通过数组创建顺序表：bool createList(SeqList& L, DataType A[],int n)
分为两种情况处理，数组长度大于或小于顺序表初始设定长度
6.查找：int Search(SeqList& L, DataType x)
7.定位：int Locate(SeqList& L,int i)
8.插入：bool Insert(SeqList& L,int i,DataType x)
9.删除：bool Remove(SeqList& L, int i, DataType x)
10.打印：void printList(SeqList& L)
11.复制：bool copylist(SeqList& L,SeqList& B)
      将L复制给B，根据B的空间是否足够进行分类
12.扩充：bool extend(SeqList& L)
      将L的空间扩充至输入长度。
13.求和：int sum(SeqList& L)


